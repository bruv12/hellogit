#include <iostream> 
#include<vector>
#include"dummy.h"
using namespace std;


int main(){

vector<Account>info;
string Name, Balance;
Account a;
int x:

 cin>> Name>>Balance>>endl;

WHILE (Name && Balance <6){
a.setName(Name);
a.setBalance(Balance);
info.push_back(a)
}
for(x=0; x<6;x++)
{
 cout<< info[x] .getName();
 cout<< info[x] .getBalance();
 }
}
